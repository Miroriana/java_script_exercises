// Write a function that checks if a given value is present in an array.
function checkValue(str,value){
    return str.includes(value);
}
const str="mignonne";
var value='a';
console.log(checkValue(str,value));