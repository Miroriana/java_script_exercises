// Write a function that finds the index of a given character in a string.
function find(string, char){
    return string.indexOf(char);
}
const string="odda";
const char="d";
console.log(find(string, char));
